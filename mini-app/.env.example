# Local Mini App env (copy to .env.local — never commit secrets)

# mock (default) | live
VITE_ALMAS_API_MODE=mock

# Required only when VITE_ALMAS_API_MODE=live
# VITE_ALMAS_API_URL=http://127.0.0.1:8787
